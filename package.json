{
  "name": "csu-deans-ep-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Public deterministic web tool that lets arts deans model their institution's exposure to the OBBBA Earnings-Premium test.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint:glossary": "tsx scripts/lint-glossary.ts",
    "lint:glossary:all": "tsx scripts/lint-glossary.ts content src/ui",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run lint:glossary && echo \"phase-4 prep — engine build lands in phase-1; this branch ships content + linter\""
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
